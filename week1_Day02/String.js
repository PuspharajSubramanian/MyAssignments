const { reverse } = require("dns")

let name="puspharaj"
let char="p"
let c=0
for(let i=0;i<=name.length;i++)
{
    if(name[i]===char)
    {
        c++
    }
}
console.log("Character p present "+c+" times")

for(let j=0; j<=name.length;j--)
{
    let rv=reverse.name
}
console.log(rv)


